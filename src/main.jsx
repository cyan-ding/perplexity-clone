import React from "react";
import { createRoot } from "react-dom/client";
import PerplexityClone from "../perplexity-clone.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PerplexityClone />
  </React.StrictMode>
);
